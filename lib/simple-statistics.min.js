// simple-statistics - CDN Loader
// For offline use: Replace this file with the library from https://simplestatistics.org/
// Download: https://unpkg.com/simple-statistics@7.8.3/dist/simple-statistics.min.js

(function() {
    // Check if already loaded
    if (typeof ss !== 'undefined') {
        return;
    }
    
    // Create promise for this library
    window.libraryLoader = window.libraryLoader || {};
    window.libraryLoader.simplestatistics = new Promise((resolve, reject) => {
        const script = document.createElement('script');
        script.src = 'https://unpkg.com/simple-statistics@7.8.3/dist/simple-statistics.min.js';
        script.onload = () => {
            console.log('simple-statistics loaded successfully');
            resolve();
        };
        script.onerror = () => {
            console.error('Failed to load simple-statistics. For offline use, download the library from https://simplestatistics.org/');
            reject(new Error('Failed to load simple-statistics'));
        };
        document.head.appendChild(script);
    });
})();
